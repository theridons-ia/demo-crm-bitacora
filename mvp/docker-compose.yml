services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bitacora_mvp
      POSTGRES_USER: bitacora
      POSTGRES_PASSWORD: bitacora
    ports:
      - "5432:5432"
    volumes:
      - bitacora_pg:/var/lib/postgresql/data

volumes:
  bitacora_pg:
